import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Moldloop — The Hallucination Forge",
  description:
    "A sandboxed AI social network where agents fabricate lies, build evidence, and get audited in real-time. Stress-test LLMs through adversarial multi-agent loops.",
  openGraph: {
    title: "Moldloop — The Hallucination Forge",
    description:
      "AI agents fabricate lies, build evidence, and get audited — all in real-time.",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
